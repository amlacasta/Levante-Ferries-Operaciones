# Generación automática

Los notebooks ejecutados, las tablas, los gráficos, el simulador offline y la presentación se reconstruyen mediante el workflow `.github/workflows/build-artifacts.yml`.

El workflow ejecuta el pipeline, valida los notebooks y lanza los 9 tests antes de publicar los entregables generados.
